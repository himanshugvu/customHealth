app:
  health:
    enabled: true
    startupLog: true
    db:
      enabled: true
      validationQuery: "SELECT 1"
      # dataSourceBean: dataSource  # Optional: specify bean name, otherwise uses type lookup
    mongodb:
      enabled: true
      # mongoTemplateBean: mongoTemplate  # Optional: specify bean name, otherwise uses type lookup
    kafka:
      enabled: true  # Test reflection-based approach
      adminClientBean: kafkaAdminClient
    external:
      services:
        - name: httpbin
          enabled: true
          restClientBean: myRestClient
          urlBean: externalServiceUrl